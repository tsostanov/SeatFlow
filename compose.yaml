services:
  postgres:
    image: postgres:18-alpine
    environment:
      POSTGRES_USER: booking
      POSTGRES_PASSWORD: booking
      POSTGRES_DB: booking
    volumes:
      - postgres-data:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U booking -d booking"]
      interval: 3s
      timeout: 3s
      retries: 20
    restart: unless-stopped

  inventory:
    build:
      context: .
      args:
        SERVICE: inventory
    environment:
      GRPC_ADDR: :50051
      DATABASE_URL: postgres://booking:booking@postgres:5432/booking?sslmode=disable
      SEED_DEMO: "true"
      GRPC_REFLECTION: ${GRPC_REFLECTION:-true}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  payment:
    build:
      context: .
      args:
        SERVICE: payment
    environment:
      GRPC_ADDR: :50053
      DATABASE_URL: postgres://booking:booking@postgres:5432/booking?sslmode=disable
      GRPC_REFLECTION: ${GRPC_REFLECTION:-true}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  notification:
    build:
      context: .
      args:
        SERVICE: notification
    environment:
      GRPC_ADDR: :50054
      DATABASE_URL: postgres://booking:booking@postgres:5432/booking?sslmode=disable
      GRPC_REFLECTION: ${GRPC_REFLECTION:-true}
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped

  booking:
    build:
      context: .
      args:
        SERVICE: booking
    environment:
      GRPC_ADDR: :50052
      INVENTORY_ADDR: inventory:50051
      PAYMENT_ADDR: payment:50053
      NOTIFICATION_ADDR: notification:50054
      BOOKING_TTL: ${BOOKING_TTL:-10m}
      GRPC_REFLECTION: ${GRPC_REFLECTION:-true}
    depends_on:
      - inventory
      - payment
      - notification
    restart: unless-stopped

  gateway:
    build:
      context: .
      args:
        SERVICE: gateway
    environment:
      HTTP_ADDR: :8080
      INVENTORY_ADDR: inventory:50051
      BOOKING_ADDR: booking:50052
      PAYMENT_ADDR: payment:50053
      NOTIFICATION_ADDR: notification:50054
    ports:
      - "127.0.0.1:${HTTP_PORT:-8080}:8080"
    depends_on:
      - inventory
      - booking
      - payment
      - notification
    healthcheck:
      test:
        ["CMD", "wget", "-q", "-O", "/dev/null", "http://127.0.0.1:8080/readyz"]
      interval: 5s
      timeout: 5s
      retries: 20
    restart: unless-stopped

volumes:
  postgres-data:
